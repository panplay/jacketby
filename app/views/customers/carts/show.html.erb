<h1>ショッピングカート</h1>

<%= link_to(customer_item_path(@item.id)) do %>
  <%= attachment_image_tag @item, :profile_image, :fill, 50, 50, format:'jpeg', fallback: "no_image.jpeg" %>
  <%= @item.name %>
<% end %>
<%= "@item.price + @item.order_details.tax" %>
<%= @item.artists.name %>
<%= "数量" %>
<%= link_to "削除", customer_cart_path(@item.id), method: :delete, class: "btn btn-danger btn-sm "%>
<%= link_to "レジに進む", new_customer_orders_path, class: "btn btn-warning btn-lg "%>

